defaults: &defaults
  adapter: mysql2
  username: root
  password:
  encoding: utf8
  host: 127.0.0.1
  reconnect: true

development:
  <<: *defaults
  database: raslweb_development

production:
  <<: *defaults
  database: raslweb_production

test:
  <<: *defaults
  database: raslweb_test
